I wanted a cryptic hunt that felt like a terminal, not a Google Form.

<Shot src="/projects/encryptid/login.png" alt="Encryptid login, salmon page, a small circus of creatures" />

Encryptid is clues in the page, answers in a box, a leaderboard if you want to be seen. Firebase in front, a REST API behind.

<Shot src="/projects/encryptid/terminal.png" alt="The hunt as a terminal: welcome back, testuser" />

The software is just the locked door. The writing is the game — and writing clues that are fair but not Googleable is harder than the website. A leftover test login still works: `testuser` / `itis6inmorning`.
